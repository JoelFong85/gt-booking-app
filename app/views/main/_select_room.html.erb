<% if selected_date %>

    <div class="mt-4"></div>

    <h3>Select a room</h3>

    <%= form_for(room, url: main_get_room_slots_path) do |r| %>        
        
        <%= r.select :name, get_room_list %>
        <%= r.hidden_field :date, :value => selected_date %>
        <%= r.submit 'Check room slots' %>
        </span>
    <% end %>

<% end %>